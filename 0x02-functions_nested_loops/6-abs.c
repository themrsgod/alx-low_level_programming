#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @num: an integer
 * Description: absoute number to compute
 * Return: integer
 */
int _abs(int num)
{
	int i = -1;
	int res;

	if (num < 0)
	{
		res = i * num;
	}
	else
	{
		res = num;
	}
	return (res);
}
