#include "main.h"

/**
 * mul - multipy two numbers.
 *
 * @first: first number.
 * @second: second number
 * Return: result.
 */
int mul(int first, int second)
{
	int result;

	result = first * second;
	return (result);
}
